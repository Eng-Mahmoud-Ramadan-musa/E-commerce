/** @type {import('next').NextConfig} */
const nextConfig = {
  
    images: {
        unoptimized: true,
    //   domains: ['cdn.dummyjson.com', 'dummyjson.com'], // أضف الدومينات المسموح بها
    },
  };

export default nextConfig;
